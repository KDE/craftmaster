# SPDX-FileCopyrightText: NONE
#
# SPDX-License-Identifier: CC0-1.0

[tox]
minversion = 4.1.3
envlist = check-imports,format,lint,reuse,sort-imports

[testenv:format]
description = runs 'ruff format'
skip_install = true
deps = ruff
commands = ruff format

[testenv:lint]
description = runs 'ruff check'
skip_install = true
deps = ruff
commands = ruff check --fix

[testenv:check-imports]
description = runs isort --check
skip_install = true
deps = isort==5.13.2
commands = isort --check --profile black .

[testenv:reuse]
description = runs 'reuse lint'
skip_install = true
deps = reuse
commands = reuse lint

[testenv:sort-imports]
description = runs isort
skip_install = true
deps = isort==5.13.2
commands = isort --profile black .

